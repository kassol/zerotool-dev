---
seoTitle: "图片马赛克在线工具 — 截图打码、模糊、涂黑 | ZeroTool"
seoDescription: "在线给图片和截图打马赛克：框选区域或整图像素化、模糊、涂黑，导出自动去除 EXIF 元数据。纯浏览器端处理，图片不上传。"
faqItems:
  - question: "马赛克能安全遮挡密码或 API Key 吗？"
    answer: "不能。小方块的像素化和模糊都是可还原的——公开工具 Depix 已经演示过，通过匹配像素块的平均色值反推出被马赛克遮挡的文字。密码、API Key、Token 等文字类机密请改用纯色涂黑。像素化更适合用大块尺寸遮挡人脸或车牌这类没有文字要还原的场景。"
  - question: "我的图片会上传吗？"
    answer: "不会。解码、效果渲染、导出全部通过 Canvas 2D 在你的浏览器本地完成。这个工具的持久化策略是 disabled，页面本身也不加载统计和广告脚本。"
  - question: "导出会去除 EXIF / GPS 元数据吗？"
    answer: "会。导出的文件由 Canvas 2D 重新编码生成，原图的 EXIF、GPS、相机型号等元数据都不会带过去，无论你打码的是哪个区域。"
  - question: "支持哪些图片格式？"
    answer: "浏览器 createImageBitmap 能解码的格式都支持，通常包括 PNG、JPEG、WebP、GIF、BMP、AVIF。HEIC 只有 Safari 能解码。动图 GIF 按设计只读取第一帧，导出为静态图。"
---

<h2>使用步骤</h2>
<ol>
  <li>把图片拖到拖拽框、点击选择文件，或直接用 <strong>Ctrl/Cmd+V</strong> 粘贴一张截图。</li>
  <li>选择一种效果：<strong>像素化</strong>、<strong>模糊</strong> 或 <strong>纯色涂黑</strong>。</li>
  <li>在图片上拖拽画出需要遮挡的区域，可以画多个，每个虚线框都是独立的区域。</li>
  <li>拖动强度滑块（像素化对应块大小，模糊对应半径），或选一个填充颜色，所有区域会实时更新效果。</li>
  <li>勾选 <strong>应用到整张图片</strong> 可以让效果作用于整张图，而不是画出的区域。</li>
  <li>点 <strong>下载</strong> 保存结果，或点 <strong>复制图片</strong> 直接粘贴到别的应用里。</li>
</ol>

<h2>三种效果该怎么选</h2>
<ul>
  <li><strong>像素化</strong> ——把每个方块内的像素平均成一个纯色。适合做马赛克风格的视觉效果，或用较大的块遮挡人脸。不适合遮挡文字。</li>
  <li><strong>模糊</strong> ——平滑柔化效果，视觉上不像"打码"，但同样可以被高强度去模糊算法还原，对文字同样不安全。</li>
  <li><strong>纯色涂黑</strong> ——用一个纯色完全覆盖该区域，是唯一彻底销毁原始像素的方式。密码、Token、API Key，以及任何要发给 AI 聊天的内容，都请用这个。</li>
</ul>

<h2>常见使用场景</h2>
<ul>
  <li><strong>把截图发给 AI 聊天、工单或论坛前脱敏</strong> ——用纯色涂黑遮住 API Key、Token、账号 ID、邮箱地址、内网地址。</li>
  <li><strong>发布照片前遮挡人脸或车牌</strong> ——用足够大的像素化块或模糊半径，抹掉可辨认的细节。</li>
  <li><strong>做复古像素风效果</strong> ——勾选"应用到整张图片"，配合较大的像素化块尺寸。</li>
</ul>

<h2>隐私：零上传</h2>
<p>
  解码、效果渲染、导出全部通过 <code>createImageBitmap</code> 和 Canvas 2D API 在浏览器本地完成，没有服务端环节参与——开飞行模式也能完整跑完。
  这个工具的持久化策略是 <code>disabled</code>：不会把图片写入本地存储，页面也不加载统计与广告脚本。
</p>

<h2>限制</h2>
<ul>
  <li>不会自动识别人脸或文字，区域需要手动框选——可靠的人脸/文字识别需要机器学习模型，这类需求交给专门的 AI 图像工具更合适。</li>
  <li>已画出的区域不能移动、缩放，也不能单独设置效果——想改就撤销重画。</li>
  <li>一次只处理一张图，没有批量模式。</li>
  <li>动图 GIF 按设计只导出静态的第一帧。</li>
  <li>不处理 PDF 打码，请用本地 PDF 编辑器自带的涂黑/编辑功能。</li>
</ul>

<h2>相关工具</h2>
<ul>
  <li><a href="/zh/tools/secret-redactor/">密钥脱敏工具</a> ——用同样的思路遮住文本和日志里的 API Key、Token，之后可以还原。</li>
  <li><a href="/zh/tools/exif-metadata-viewer/">EXIF 元数据查看器</a> ——查看或去除 EXIF/GPS 信息，不改动图片本身。</li>
  <li><a href="/zh/tools/image-compressor/">图片压缩工具</a> ——打码之后再压缩一下截图体积。</li>
</ul>
