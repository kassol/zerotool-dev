---
seoTitle: "Pixelate Image Online — Blur & Redact Screenshots | ZeroTool"
seoDescription: "Pixelate or blur faces, text and secrets in images and screenshots. Select areas or the whole image, black out passwords, strip metadata. No upload."
faqItems:
  - question: "Is pixelation safe for hiding passwords or API keys?"
    answer: "No. Small-block pixelation and blur are reversible — the public tool Depix has demonstrated recovering readable text from pixelated screenshots by matching the averaged blocks against known font patterns. For passwords, API keys, tokens, or any text secret, use Solid fill instead. Pixelate and blur suit a mosaic look or casual obscuring. When a face, license plate or any text must stay unrecoverable, use Solid fill."
  - question: "Are my images uploaded anywhere?"
    answer: "No. Everything runs on Canvas 2D in your browser — decoding, effect rendering, and export all happen locally. This tool's persistence policy is disabled, so it also skips loading analytics and ad scripts on this page."
  - question: "Does it remove EXIF and GPS metadata?"
    answer: "Yes. The exported file is a fresh re-encode produced by the Canvas 2D API, so none of the original file's EXIF, GPS, or camera metadata carries over — regardless of which area you pixelate."
  - question: "Which image formats are supported?"
    answer: "Any format your browser's createImageBitmap can decode — typically PNG, JPEG, WebP, GIF, BMP, and AVIF. HEIC only decodes in Safari. Animated GIFs are read as their first frame only and exported as a static image, by design."
---

<h2>How to Use</h2>
<ol>
  <li>Drop an image on the drop zone, click to pick a file, or paste a screenshot with <strong>Ctrl/Cmd+V</strong>.</li>
  <li>Choose an effect: <strong>Pixelate</strong>, <strong>Blur</strong>, or <strong>Solid fill</strong>.</li>
  <li>Drag a rectangle over each area you want to hide. Draw as many as you need — each dashed box is a separate area.</li>
  <li>Adjust the strength slider (block size for Pixelate, radius for Blur) or pick a fill color. Every area updates instantly.</li>
  <li>Enable <strong>Apply to entire image</strong> to affect the whole picture instead of drawn areas.</li>
  <li>Click <strong>Download</strong> to save the result, or <strong>Copy image</strong> to paste it straight into another app.</li>
</ol>

<h2>Which Effect Should You Use?</h2>
<ul>
  <li><strong>Pixelate</strong> — averages each block of pixels into a flat color. Good for a deliberate mosaic look or casual obscuring. Not safe for text, and a large block size does not guarantee a face cannot be recognized.</li>
  <li><strong>Blur</strong> — scales the area down and back up, producing a soft, smeared look. Reads as less "censored" than pixelation but is just as reversible with strong enough deblurring, and equally unsafe for text.</li>
  <li><strong>Solid fill</strong> — replaces the area with one flat color. The only option here that destroys the underlying pixels completely. Always use this for passwords, tokens, API keys, and anything you paste into an AI chat.</li>
</ul>

<h2>Common Use Cases</h2>
<ul>
  <li><strong>Sanitizing screenshots before pasting into an AI chat, a support ticket, or a public forum</strong> — black out API keys, tokens, account IDs, email addresses, and internal URLs with Solid fill.</li>
  <li><strong>Blurring faces or license plates</strong> before publishing a photo. Use Solid fill when identity must stay hidden — recognition models can still match pixelated or blurred faces.</li>
  <li><strong>Retro pixel-art effects</strong> — check "Apply to entire image" with a large Pixelate block size for a deliberate low-res look.</li>
</ul>

<h2>Privacy: Nothing Is Uploaded</h2>
<p>
  Decoding, effect rendering, and export all run through <code>createImageBitmap</code> and the Canvas 2D API directly in your browser.
  There is no server endpoint involved — try it in airplane mode to confirm. This tool's persistence policy is <code>disabled</code>: it never
  writes your image to local storage, and this page does not load analytics or ad scripts.
</p>

<h2>Limits</h2>
<ul>
  <li>No automatic face or text detection — you draw the areas yourself. Detecting faces or text reliably needs a machine-learning model, which is a better job for an AI image tool than a lightweight browser utility.</li>
  <li>Areas cannot be moved, resized, or given independent effects after being drawn — undo and redraw instead.</li>
  <li>One image at a time; there is no batch mode for multiple files.</li>
  <li>Animated GIFs export as a single static frame, by design.</li>
  <li>PDF redaction is out of scope — use a local PDF editor's redact or black-out feature instead.</li>
</ul>

<h2>Related Tools</h2>
<ul>
  <li><a href="/tools/secret-redactor/">Secret Redactor</a> — mask API keys and tokens in text and logs the same way, then restore them later.</li>
  <li><a href="/tools/exif-metadata-viewer/">EXIF Metadata Viewer</a> — inspect or strip EXIF/GPS data without altering the image itself.</li>
  <li><a href="/tools/image-compressor/">Image Compressor</a> — shrink a screenshot's file size after redacting it.</li>
</ul>
