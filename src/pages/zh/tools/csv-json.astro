---
import ToolLayout from '../../../layouts/ToolLayout.astro';
import CsvJsonTool from '../../../components/tools/CsvJsonTool.astro';
---

<ToolLayout
  toolName="CSV JSON 转换器"
  title="CSV JSON 互转工具 — 免费数据格式转换 | ZeroTool"
  description="即时进行 CSV 与 JSON 格式互转。支持引号字段，自动识别表头。免费在线工具，纯浏览器端运行。"
  lang="zh"
  faqItems={[
    {
      question: "支持什么 CSV 格式？",
      answer: "标准 RFC 4180 CSV：逗号分隔值，支持双引号转义。引号字段中可包含逗号、换行符和转义引号（\"\"）。",
    },
    {
      question: "表头如何处理？",
      answer: "CSV 的第一行始终作为表头。之后每一行都会转换为以表头为键的 JSON 对象。",
    },
    {
      question: "有文件大小限制吗？",
      answer: "没有硬性限制——所有处理都在浏览器中完成。实际使用中，几 MB 以内的文件可以即时转换；更大的文件取决于设备性能。",
    },
    {
      question: "数据会发送到服务器吗？",
      answer: "不会。所有转换完全在浏览器中通过 JavaScript 运行，数据不会离开您的设备。",
    },
  ]}
>
  <CsvJsonTool slot="tool" />

  <h2>使用方法</h2>
  <ol>
    <li><strong>CSV → JSON</strong>：将带表头的 CSV 数据粘贴到左侧面板，点击 <strong>CSV → JSON</strong> 按钮。</li>
    <li><strong>JSON → CSV</strong>：将 JSON 对象数组粘贴到右侧面板，点击 <strong>JSON → CSV</strong> 按钮。</li>
    <li>点击面板上的 <strong>Copy</strong> 按钮，将结果复制到剪贴板。</li>
    <li>按钮下方的状态栏会显示转换行数或解析错误信息。</li>
  </ol>

  <h2>转换示例</h2>

  <h3>简单表格</h3>
  <p>输入 CSV：</p>
  <pre><code>name,age,city
Alice,30,London
Bob,25,Paris</code></pre>
  <p>输出 JSON：</p>
  <pre><code set:html={`[\n  { "name": "Alice", "age": "30", "city": "London" },\n  { "name": "Bob", "age": "25", "city": "Paris" }\n]`} /></pre>

  <h3>含逗号的引号字段</h3>
  <p>输入 CSV：</p>
  <pre><code>product,description,price
Widget,"Small, round widget",9.99
Gadget,"Includes ""turbo"" mode",24.50</code></pre>
  <p><code>description</code> 字段包含逗号和转义引号——均可正确处理。</p>

  <h3>JSON 转回 CSV</h3>
  <p>将 JSON 输出粘贴到右侧面板，点击 <strong>JSON → CSV</strong> 即可将数据往返转换。</p>

  <h2>支持的 CSV 格式</h2>
  <p>
    本工具遵循 <strong>RFC 4180</strong> 标准。字段以逗号分隔。
    若字段中包含逗号、换行符或双引号，则必须用双引号包裹。
    引号字段内的双引号以两个连续双引号（<code>""</code>）转义。
    第一行始终作为列标题解析。
  </p>
</ToolLayout>
