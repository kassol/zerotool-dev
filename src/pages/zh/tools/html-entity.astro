---
import ToolLayout from '../../../layouts/ToolLayout.astro';
import HtmlEntityTool from '../../../components/tools/HtmlEntityTool.astro';
---

<ToolLayout
  toolName="HTML 实体编码 / 解码"
  title="HTML 实体编码解码工具 — 免费在线转换 | ZeroTool"
  description="即时编码和解码 HTML 实体。支持命名实体、十进制和十六进制编码。免费在线工具，纯浏览器端运行。"
  lang="zh"
  faqItems={[
    {
      question: "什么是 HTML 实体？",
      answer: "HTML 实体是用于表示在 HTML 中有特殊含义或键盘上无法直接输入的字符的代码。例如 < 写成 &amp;lt;，这样浏览器不会将其解释为 HTML 标签。",
    },
    {
      question: "什么时候需要编码 HTML 实体？",
      answer: "当你显示用户生成的内容、在 HTML 中嵌入代码片段，或需要在 HTML 属性和文本中安全地包含 <、>、& 和引号等特殊字符时，应当进行编码以防止 XSS 攻击和渲染错误。",
    },
    {
      question: "命名实体、十进制实体和十六进制实体有什么区别？",
      answer: "命名实体使用人类可读的标签（如 &amp;amp;）。十进制实体使用字符的 Unicode 码点的十进制表示（如 &amp;#38;）。十六进制实体使用十六进制表示（如 &amp;#x26;）。浏览器支持所有三种格式。",
    },
    {
      question: "我的数据会发送到服务器吗？",
      answer: "不会。所有编码和解码完全在浏览器中通过 JavaScript 完成，数据不会离开你的设备。",
    },
  ]}
>
  <HtmlEntityTool slot="tool" />

  <h2>使用方法</h2>
  <ol>
    <li>将文本或 HTML 编码字符串粘贴到<strong>输入框</strong>中。</li>
    <li>点击<strong>编码</strong>将特殊字符转换为 HTML 实体，或点击<strong>解码</strong>将实体还原为字符。</li>
    <li>点击<strong>复制</strong>将结果复制到剪贴板。</li>
  </ol>

  <h2>示例演示</h2>

  <h3>编码代码片段以供展示</h3>
  <p>
    你希望在网页上将 <code>&lt;div class="box"&gt;</code> 作为可见文本显示。
    将其粘贴到输入框并点击<strong>编码</strong>，输出为：
  </p>
  <pre><code>&amp;lt;div class=&amp;quot;box&amp;quot;&amp;gt;</code></pre>
  <p>这样可以在任何浏览器中安全地渲染为字面量 HTML 标签文本。</p>

  <h3>解码接收到的 HTML 内容</h3>
  <p>
    你从 API 收到字符串：<code>Tom &amp;amp; Jerry &amp;mdash; Season 1</code>。
    将其粘贴并点击<strong>解码</strong>，输出为：
  </p>
  <pre><code>Tom &amp; Jerry — Season 1</code></pre>

  <h3>编码非 ASCII 字符</h3>
  <p>
    粘贴 <code>Caf&eacute; &copy; 2024</code> 并点击<strong>编码</strong>，
    非 ASCII 字符将被转换为数值实体：
  </p>
  <pre><code>Caf&amp;#233; &amp;#169; 2024</code></pre>

  <h2>常见 HTML 实体</h2>
  <table>
    <thead>
      <tr><th>字符</th><th>实体名称</th><th>十进制</th><th>说明</th></tr>
    </thead>
    <tbody>
      <tr><td><code>&amp;</code></td><td><code>&amp;amp;</code></td><td><code>&amp;#38;</code></td><td>与号</td></tr>
      <tr><td><code>&lt;</code></td><td><code>&amp;lt;</code></td><td><code>&amp;#60;</code></td><td>小于号</td></tr>
      <tr><td><code>&gt;</code></td><td><code>&amp;gt;</code></td><td><code>&amp;#62;</code></td><td>大于号</td></tr>
      <tr><td><code>"</code></td><td><code>&amp;quot;</code></td><td><code>&amp;#34;</code></td><td>双引号</td></tr>
      <tr><td><code>'</code></td><td><code>&amp;apos;</code></td><td><code>&amp;#39;</code></td><td>单引号</td></tr>
      <tr><td>&nbsp;</td><td><code>&amp;nbsp;</code></td><td><code>&amp;#160;</code></td><td>不换行空格</td></tr>
      <tr><td><code>&copy;</code></td><td><code>&amp;copy;</code></td><td><code>&amp;#169;</code></td><td>版权符号</td></tr>
      <tr><td><code>&trade;</code></td><td><code>&amp;trade;</code></td><td><code>&amp;#8482;</code></td><td>商标符号</td></tr>
      <tr><td><code>&mdash;</code></td><td><code>&amp;mdash;</code></td><td><code>&amp;#8212;</code></td><td>长破折号</td></tr>
      <tr><td><code>&euro;</code></td><td><code>&amp;euro;</code></td><td><code>&amp;#8364;</code></td><td>欧元符号</td></tr>
    </tbody>
  </table>
</ToolLayout>
