---
import ToolLayout from '../../../layouts/ToolLayout.astro';
import MarkdownTableGeneratorTool from '../../../components/tools/MarkdownTableGeneratorTool.astro';
---

<ToolLayout
  toolName="Markdown 表格生成器"
  title="Markdown 表格生成器 — 可视化编辑，支持 CSV 和 JSON 导入 | ZeroTool"
  description="可视化构建 Markdown 表格，支持内联编辑、行列增删、列对齐设置，粘贴 CSV 或 JSON 数据即可自动生成 GFM 格式表格。"
  lang="zh"
  faqItems={[
    {
      question: "Markdown 表格的语法是什么？",
      answer: "GitHub Flavored Markdown（GFM）表格使用竖线符号分隔列。第一行是表头，第二行是分隔符（如 |---|---|），后续行为数据行。示例：| 姓名 | 年龄 |\\n|---|---|\\n| Alice | 30 |",
    },
    {
      question: "如何设置列对齐方式？",
      answer: "列对齐通过分隔符行的冒号来控制：:--- 左对齐（默认），:---: 居中，---: 右对齐。点击表头行中的 ⇤ ⇔ ⇥ 按钮即可切换对齐方式。",
    },
    {
      question: "可以粘贴 CSV 数据吗？",
      answer: "可以。切换到「CSV 粘贴」标签页，粘贴 CSV 内容。工具会自动识别分隔符（逗号、制表符或分号），并立即填充可视化表格和 Markdown 输出。",
    },
    {
      question: "支持什么 JSON 格式？",
      answer: "粘贴一个 JSON 对象数组——每个对象的键作为列标题，每个对象作为一行数据。示例：[{\"name\":\"Alice\",\"age\":30},{\"name\":\"Bob\",\"age\":25}]",
    },
    {
      question: "单元格内的竖线符号会如何处理？",
      answer: "单元格内容中的竖线符号（|）会自动转义为 \\|，确保生成的 Markdown 表格格式正确。",
    },
  ]}
>
  <MarkdownTableGeneratorTool slot="tool" />

  <h2>使用方法</h2>

  <h3>手动编辑器</h3>
  <ol>
    <li>点击任意单元格直接编辑，Markdown 输出实时更新。</li>
    <li>使用 <strong>+ Row</strong> / <strong>+ Column</strong> 添加行或列，使用 <strong>− Row</strong> / <strong>− Column</strong> 删除行或列。</li>
    <li>在表头行中，点击 <strong>⇤ ⇔ ⇥</strong> 按钮设置列对齐方式（左 / 居中 / 右）。</li>
    <li>点击 <strong>Copy</strong> 将 Markdown 表格复制到剪贴板。</li>
  </ol>

  <h3>CSV 粘贴</h3>
  <ol>
    <li>切换到 <strong>CSV 粘贴</strong> 标签页。</li>
    <li>粘贴 CSV 数据——第一行作为表头。</li>
    <li>工具自动识别逗号、制表符或分号作为分隔符，立即填充表格。</li>
  </ol>

  <h3>JSON 粘贴</h3>
  <ol>
    <li>切换到 <strong>JSON 粘贴</strong> 标签页。</li>
    <li>粘贴一个 JSON 对象数组，键作为列标题，每个对象作为一行数据。</li>
    <li>输入时表格和 Markdown 输出即时更新。</li>
  </ol>

  <h2>支持的格式</h2>
  <p>
    本工具生成符合 <strong>GitHub Flavored Markdown（GFM）</strong> 标准的表格，
    兼容 GitHub、GitLab、Notion、Obsidian、VS Code 及大多数现代 Markdown 渲染器。
    单元格内容中的竖线符号（<code>|</code>）会自动转义。
  </p>

  <h2>示例</h2>

  <h3>基础表格</h3>
  <p>手动编辑器，3 列 2 数据行：</p>
  <pre><code>| 姓名  | 年龄 | 城市   |
|-------|------|--------|
| Alice | 30   | 伦敦   |
| Bob   | 25   | 巴黎   |</code></pre>

  <h3>居中对齐列</h3>
  <p>点击「分数」表头的 ⇔ 按钮设置居中对齐：</p>
  <pre><code>| 玩家  | 分数  | 等级 |
|-------|:-----:|------|
| Alice |  100  | 5    |
| Bob   |   80  | 3    |</code></pre>

  <h3>从 JSON 导入</h3>
  <p>在 JSON 粘贴标签页中粘贴以下内容：</p>
  <pre><code set:html={`[{"product":"Widget","price":9.99},{"product":"Gadget","price":24.50}]`} /></pre>
  <p>生成结果：</p>
  <pre><code>| product | price |
|---------|-------|
| Widget  | 9.99  |
| Gadget  | 24.5  |</code></pre>
</ToolLayout>
